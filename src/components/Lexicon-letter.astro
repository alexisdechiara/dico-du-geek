---
export interface Props {
	letter: string;
}

const { letter } = Astro.props;
---

<a href={`/lexique/${letter}`} class="flex justify-center items-center text-lexicon-word uppercase hover:bg-secondary hover:text-background rounded-lg transition-colors px-4"
	>{letter}</a
>
